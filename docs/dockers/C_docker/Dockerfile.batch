# 1. 베이스 이미지 선택
FROM python:3.10-slim-bullseye

# 2. 필수 시스템 패키지 및 Oracle Instant Client 설치
RUN apt-get update && apt-get install -y \
    libaio1 \
    libpq-dev \
    gcc \
    curl \
    unzip \
    && rm -rf /var/lib/apt/lists/*

# 3. 작업 디렉토리 설정
WORKDIR /app

# 4. 의존성 파일 복사 및 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 5. 소스 코드 복사
COPY . .

# 6. 환경 변수 설정 (Oracle 접속용)
ENV LD_LIBRARY_PATH=/usr/lib/oracle/client

# 7. 실행 명령
CMD ["python", "main_batch.py"]