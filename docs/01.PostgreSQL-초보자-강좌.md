# PostgreSQL 초보자 강좌

## 1. PostgreSQL이란?

PostgreSQL은 **오픈소스 관계형 데이터베이스 관리 시스템(RDBMS)**입니다.

### 특징

- 무료 오픈소스
- 안정성과 신뢰성이 뛰어남
- 복잡한 쿼리와 대용량 데이터 처리에 강함
- JSON, 배열 등 다양한 데이터 타입 지원
- 확장성이 좋음

---

## 2. 설치하기

### Windows

1. [공식 사이트](https://www.postgresql.org/download/windows/)에서 설치 파일 다운로드
2. 설치 마법사 실행
3. 비밀번호 설정 (꼭 기억하기!)
4. 기본 포트: 5432

### 윈도우 설정 화면에서 직접 환경 변수를 추가하는 방법

1. 윈도우 왼쪽밑 찾기에서 '시스템' 검색 "시스템 환경 변수 편집" 클릭
2. 시스템 변수 창 오른쪽 아래 [환경 변수(N)...] 클릭
3. 사용자 변수에서 Path 선택 후 [편집(E)...] 클릭
4. [새로 만들기(N)] 클릭 후 C:\Program Files\PostgreSQL\18\bin 입력
5. [확인] 버튼을 눌러 창을 닫습니다.
6. 나머지 열려있는 창들도 모두 [확인]을 눌러서 닫습니다.
7. vscode 안티그래피티 다 껏다가 다시 켜고 터미널 열고 `psql --version`을 입력했을 때 버전 정보가 나오면 성공입니다.
---

## 3. 기본 접속 방법

### 터미널에서 접속

```bash
# 기본 사용자(postgres)로 접속
$ psql -U postgres

# 특정 데이터베이스에 접속
$ psql -U 사용자명 -d 데이터베이스명

# 호스트 지정 접속
$ psql -h localhost -U postgres -d mydb
```

### 접속 후 기본 명령어

```sql
-- 현재 접속 정보 확인
\conninfo

-- 도움말
\?

-- SQL 명령어 도움말
\h

-- 종료
\q
```

---

## 4. 데이터베이스 관리

### 데이터베이스 목록 보기

```sql
-- psql 명령어
\l

-- SQL 쿼리
SELECT datname FROM pg_database;
```

### 데이터베이스 생성

```sql
CREATE DATABASE mydb;

-- 인코딩 지정
CREATE DATABASE mydb
  WITH ENCODING 'UTF8'
  LC_COLLATE='Korean_Korea'
  LC_CTYPE='Korean_Korea'
  TEMPLATE template0;
```

### 데이터베이스 삭제

```sql
DROP DATABASE mydb;
```

### 데이터베이스 전환

```sql
\c mydb
-- 또는
\connect mydb
```

---

## 5. 테이블 관리

### 테이블 목록 보기

```sql
-- 모든 테이블
\dt

-- 상세 정보
\dt+

-- 특정 테이블 구조 보기
\d 테이블명
```

### 테이블 생성

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,          -- 자동 증가 정수
    username VARCHAR(50) NOT NULL,  -- 가변 문자열, 필수
    email VARCHAR(100) UNIQUE,      -- 유일값
    age INTEGER,                    -- 정수
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP  -- 기본값
);
```

### 주요 데이터 타입

| 타입           | 설명                   | 예시                  |
| -------------- | ---------------------- | --------------------- |
| `INTEGER`      | 정수                   | 1, 100, -50           |
| `SERIAL`       | 자동 증가 정수         | 1, 2, 3...            |
| `VARCHAR(n)`   | 가변 문자열 (최대 n자) | 'hello'               |
| `TEXT`         | 긴 문자열              | 긴 텍스트             |
| `BOOLEAN`      | 참/거짓                | TRUE, FALSE           |
| `DATE`         | 날짜                   | '2026-01-25'          |
| `TIMESTAMP`    | 날짜+시간              | '2026-01-25 10:30:00' |
| `NUMERIC(p,s)` | 정밀 소수              | 123.45                |
| `JSON/JSONB`   | JSON 데이터            | '{"key": "value"}'    |

### 테이블 수정

```sql
-- 컬럼 추가
ALTER TABLE users ADD COLUMN phone VARCHAR(20);

-- 컬럼 삭제
ALTER TABLE users DROP COLUMN phone;

-- 컬럼 타입 변경
ALTER TABLE users ALTER COLUMN age TYPE BIGINT;

-- 컬럼명 변경
ALTER TABLE users RENAME COLUMN username TO name;
```

### 테이블 삭제

```sql
DROP TABLE users;

-- 존재할 때만 삭제 (에러 방지)
DROP TABLE IF EXISTS users;
```

---

## 6. 데이터 조작 (CRUD)

### CREATE - 데이터 삽입

```sql
-- 단일 행 삽입
INSERT INTO users (username, email, age)
VALUES ('홍길동', 'hong@example.com', 25);

-- 여러 행 삽입
INSERT INTO users (username, email, age)
VALUES
    ('김철수', 'kim@example.com', 30),
    ('이영희', 'lee@example.com', 28);

-- 모든 컬럼에 값 삽입 (컬럼명 생략)
INSERT INTO users
VALUES (DEFAULT, '박민수', 'park@example.com', 35, DEFAULT);
```

### READ - 데이터 조회

```sql
-- 모든 데이터 조회
SELECT * FROM users;

-- 특정 컬럼만 조회
SELECT username, email FROM users;

-- 조건 조회 (WHERE)
SELECT * FROM users WHERE age > 25;

-- 정렬 (ORDER BY)
SELECT * FROM users ORDER BY age DESC;  -- 내림차순
SELECT * FROM users ORDER BY username ASC;  -- 오름차순

-- 개수 제한 (LIMIT)
SELECT * FROM users LIMIT 10;

-- 건너뛰기 (OFFSET) - 페이징에 유용
SELECT * FROM users LIMIT 10 OFFSET 20;

-- 중복 제거
SELECT DISTINCT age FROM users;

-- 개수 세기
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM users WHERE age > 25;
```

### UPDATE - 데이터 수정

```sql
-- 특정 조건의 데이터 수정
UPDATE users
SET age = 26
WHERE username = '홍길동';

-- 여러 컬럼 수정
UPDATE users
SET age = 27, email = 'new@example.com'
WHERE id = 1;
```

> ⚠️ **주의**: WHERE 절 없이 UPDATE하면 모든 행이 수정됩니다!

### DELETE - 데이터 삭제

```sql
-- 특정 조건의 데이터 삭제
DELETE FROM users WHERE id = 1;

-- 모든 데이터 삭제 (주의!)
DELETE FROM users;

-- 테이블 비우기 (더 빠름)
TRUNCATE TABLE users;

-- 인덱스도 초기화
TRUNCATE TABLE users RESTART IDENTITY;
```

> ⚠️ **주의**: WHERE 절 없이 DELETE하면 모든 데이터가 삭제됩니다!

---

## 7. 조건과 연산자

### 비교 연산자

```sql
SELECT * FROM users WHERE age = 25;    -- 같다
SELECT * FROM users WHERE age != 25;   -- 같지 않다
SELECT * FROM users WHERE age <> 25;   -- 같지 않다
SELECT * FROM users WHERE age > 25;    -- 크다
SELECT * FROM users WHERE age >= 25;   -- 크거나 같다
SELECT * FROM users WHERE age < 25;    -- 작다
SELECT * FROM users WHERE age <= 25;   -- 작거나 같다
```

### 논리 연산자

```sql
-- AND (그리고)
SELECT * FROM users WHERE age > 20 AND age < 30;

-- OR (또는)
SELECT * FROM users WHERE age < 20 OR age > 30;

-- NOT (부정)
SELECT * FROM users WHERE NOT age = 25;
```

### 특수 연산자

```sql
-- BETWEEN (범위)
SELECT * FROM users WHERE age BETWEEN 20 AND 30;

-- IN (목록 중 하나)
SELECT * FROM users WHERE age IN (25, 30, 35);

-- LIKE (패턴 매칭)
SELECT * FROM users WHERE username LIKE '김%';    -- '김'으로 시작
SELECT * FROM users WHERE email LIKE '%@gmail.com';  -- gmail로 끝남
SELECT * FROM users WHERE username LIKE '%길%';   -- '길' 포함

-- IS NULL / IS NOT NULL
SELECT * FROM users WHERE email IS NULL;
SELECT * FROM users WHERE email IS NOT NULL;
```

---

## 8. 집계 함수

```sql
-- 개수
SELECT COUNT(*) FROM users;

-- 합계
SELECT SUM(age) FROM users;

-- 평균
SELECT AVG(age) FROM users;

-- 최대값
SELECT MAX(age) FROM users;

-- 최소값
SELECT MIN(age) FROM users;

-- 그룹별 집계 (GROUP BY)
SELECT age, COUNT(*) as count
FROM users
GROUP BY age;

-- 그룹 조건 (HAVING)
SELECT age, COUNT(*) as count
FROM users
GROUP BY age
HAVING COUNT(*) > 1;
```

---

## 9. 조인 (JOIN)

### 테이블 준비 예시

```sql
-- 사용자 테이블
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50)
);

-- 주문 테이블
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    product VARCHAR(100),
    amount INTEGER
);
```

### INNER JOIN (교집합)

```sql
-- 두 테이블 모두에 있는 데이터만
SELECT users.name, orders.product, orders.amount
FROM users
INNER JOIN orders ON users.id = orders.user_id;
```

### LEFT JOIN (왼쪽 기준)

```sql
-- 왼쪽 테이블의 모든 데이터 + 매칭되는 오른쪽 데이터
SELECT users.name, orders.product
FROM users
LEFT JOIN orders ON users.id = orders.user_id;
```

### RIGHT JOIN (오른쪽 기준)

```sql
-- 오른쪽 테이블의 모든 데이터 + 매칭되는 왼쪽 데이터
SELECT users.name, orders.product
FROM users
RIGHT JOIN orders ON users.id = orders.user_id;
```

### 테이블 별칭 사용

```sql
SELECT u.name, o.product, o.amount
FROM users u
INNER JOIN orders o ON u.id = o.user_id
WHERE o.amount > 10000;
```

---

## 10. 인덱스

### 인덱스란?

데이터 검색 속도를 높이는 자료구조입니다. 책의 색인과 같은 역할을 합니다.

### 인덱스 생성

```sql
-- 단일 컬럼 인덱스
CREATE INDEX idx_users_email ON users(email);

-- 복합 인덱스
CREATE INDEX idx_users_name_age ON users(username, age);

-- 유니크 인덱스
CREATE UNIQUE INDEX idx_users_email_unique ON users(email);
```

### 인덱스 삭제

```sql
DROP INDEX idx_users_email;
```

### 인덱스 목록 보기

```sql
\di
```

> 💡 **팁**: WHERE 절에서 자주 사용하는 컬럼에 인덱스를 만들면 검색 속도가 빨라집니다.

---

## 11. 사용자 관리

### 사용자 생성

```sql
-- 1. 유저 생성 (어디서든 가능)
CREATE USER myuser WITH PASSWORD 'mypassword';

-- 2. 데이터베이스 접속 권한 부여 (어디서든 가능)
GRANT ALL PRIVILEGES ON DATABASE mydb TO myuser;

-- 3. ★ mydb로 이동 (이게 빠져서 안 됐을 겁니다)
\c mydb

-- 4. 이제 테이블이 보이므로 권한 부여 가능
GRANT SELECT, INSERT, UPDATE ON users TO myuser;

-- 5. 스키마의 모든 테이블 권한 부여
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO myuser;
```

### 권한 취소

```sql
REVOKE ALL PRIVILEGES ON DATABASE mydb FROM myuser;
```

### 사용자 삭제

```sql
DROP USER myuser;
```

### 사용자 목록

```sql
\du
```

---

## 12. 백업과 복원

### 백업 (pg_dump)

```bash
# 단일 데이터베이스 백업
$ pg_dump -U postgres mydb > backup.sql

# 압축 백업
$ pg_dump -U postgres mydb | gzip > backup.sql.gz

# 특정 테이블만 백업
$ pg_dump -U postgres -t users mydb > users_backup.sql
```

### 복원 (psql)

```bash
# SQL 파일 복원
$ psql -U postgres -d mydb < backup.sql

# 압축 파일 복원
$ gunzip -c backup.sql.gz | psql -U postgres -d mydb
```

---

## 13. 유용한 psql 명령어 모음

| 명령어         | 설명                     |
| -------------- | ------------------------ |
| `\l`           | 데이터베이스 목록        |
| `\c dbname`    | 데이터베이스 전환        |
| `\dt`          | 테이블 목록              |
| `\d tablename` | 테이블 구조              |
| `\di`          | 인덱스 목록              |
| `\du`          | 사용자 목록              |
| `\df`          | 함수 목록                |
| `\dn`          | 스키마 목록              |
| `\timing`      | 쿼리 실행 시간 표시 토글 |
| `\x`           | 확장 출력 모드 토글      |
| `\i filename`  | SQL 파일 실행            |
| `\o filename`  | 출력을 파일로 저장       |
| `\q`           | 종료                     |

---

## 14. 자주 하는 실수와 해결

### 1. 세미콜론 빠뜨림

```sql
-- 잘못된 예
SELECT * FROM users

-- 올바른 예
SELECT * FROM users;
```

### 2. 문자열에 작은따옴표 사용

```sql
-- 잘못된 예 (큰따옴표)
SELECT * FROM users WHERE name = "홍길동";

-- 올바른 예 (작은따옴표)
SELECT * FROM users WHERE name = '홍길동';
```

### 3. WHERE 절 없이 UPDATE/DELETE

```sql
-- 위험! 모든 데이터 수정됨
UPDATE users SET age = 25;

-- 안전한 방법
UPDATE users SET age = 25 WHERE id = 1;
```

### 4. 대소문자 구분

```sql
-- PostgreSQL은 기본적으로 소문자로 처리
-- 대문자 테이블명을 사용하려면 큰따옴표 필요
SELECT * FROM "Users";  -- 대문자 테이블
SELECT * FROM users;    -- 소문자 테이블
```

---
 
## 참고 자료

- [PostgreSQL 공식 문서](https://www.postgresql.org/docs/)
- [PostgreSQL 튜토리얼](https://www.postgresqltutorial.com/)
