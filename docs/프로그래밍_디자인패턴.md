# 프로그래밍 디자인 패턴

프로그래밍 디자인 패턴은 소프트웨어 설계 과정에서 반복적으로 발생하는 문제들에 대해 검증된 **'템플릿'**과 같습니다. 1994년 GoF(Gang of Four)에 의해 정리된 23개의 패턴이 가장 대표적이며, 크게 세 가지 카테고리로 나뉩니다.

사용자님이 구축 중인 **AI 모의 면접 시스템**과 같은 복잡한 아키텍처에서 특히 유용한 패턴들을 중심으로 목록을 정리해 드립니다.

---

## 1. 생성 패턴 (Creational Patterns)

객체의 **생성 방식**을 결정하는 패턴입니다. 객체 생성 로직을 캡슐화하여 시스템의 유연성을 높입니다.

* **싱글톤 (Singleton):** 시스템 전체에서 클래스의 인스턴스를 단 하나만 생성하도록 보장합니다. (예: DB 연결 풀, 설정 관리자)
* **팩토리 메서드 (Factory Method):** 객체 생성 처리를 서브 클래스로 분리하여, 어떤 클래스의 인스턴스를 만들지 결정합니다. (예: 인터뷰 질문 유형에 따라 다른 평가 모델 생성)
* **추상 팩토리 (Abstract Factory):** 연관된 객체들의 군을 생성하기 위한 인터페이스를 제공합니다.
* **빌더 (Builder):** 복잡한 객체의 생성 단계와 표현 단계를 분리합니다. (예: 면접 결과 리포트 생성 시 점수, 피드백, 그래프 등을 단계별로 조합)
* **프로토타입 (Prototype):** 기존 객체를 복사하여 새로운 객체를 생성합니다.

---

## 2. 구조 패턴 (Structural Patterns)

클래스나 객체들을 조합하여 **더 큰 구조**를 만드는 패턴입니다.

* **어댑터 (Adapter):** 호환되지 않는 인터페이스를 가진 객체들이 함께 작동할 수 있도록 변환해 줍니다. (예: 서로 다른 STT 라이브러리의 출력을 공통 형식으로 변환)
* **프록시 (Proxy):** 실제 객체에 대한 접근을 제어하거나 부가 기능을 수행하는 대리인을 둡니다. (예: 무거운 AI 모델 로딩 전의 가상 객체)
* **데코레이터 (Decorator):** 객체에 동적으로 새로운 책임(기능)을 추가합니다. (예: 질문 생성 함수에 로그 기록 기능 추가)
* **파사드 (Facade):** 복잡한 서브시스템에 대해 단순한 인터페이스를 제공합니다. (예: 여러 AI 엔진을 하나로 묶어 처리하는 인터페이스)
* **브릿지 (Bridge):** 구현부에서 추상층을 분리하여 독립적으로 확장하게 합니다.

---

## 3. 행위 패턴 (Behavioral Patterns)

객체 사이의 **알고리즘이나 책임 분배**에 관한 패턴입니다.

* **옵저버 (Observer):** 한 객체의 상태 변화를 관찰자들에게 알리고 자동 업데이트합니다. (예: 면접 종료 이벤트 발생 시 점수 계산과 알림 전송 동시 실행)
* **전략 (Strategy):** 알고리즘을 캡슐화하여 런타임에 교체 가능하게 만듭니다. (예: 면접 난이도(쉬움/보통/어려움)에 따른 질문 선택 알고리즘 교체)
* **상태 (State):** 객체 내부 상태에 따라 행위를 변경합니다. (예: 면접 단계(대기/진행/종료)에 따른 인터페이스 변경)
* **커맨드 (Command):** 요청을 객체 형태로 캡슐화하여 매개변수화하거나 취소할 수 있게 합니다.
* **템플릿 메서드 (Template Method):** 알고리즘의 골격은 부모 클래스에 정의하고, 구체적인 단계는 자식 클래스에서 구현합니다.

---

## 💡 현재 프로젝트에 추천하는 우선순위 패턴

사용자님의 **3대 서버 클러스터링 기반 AI 면접 시스템**에는 다음 패턴들을 먼저 검토해 보시는 것을 추천합니다.

1. **프록시 패턴:** 서버 B에서 무거운 AI 모델을 호출할 때 네트워크 지연이나 메모리 관리를 위해 필수적입니다.
2. **전략 패턴:** 면접 분야(IT, 마케팅 등)나 난이도에 따라 질문 추출 로직을 유연하게 바꿔야 할 때 유용합니다.
3. **옵저버 패턴:** 면접자가 답변을 완료했을 때(이벤트), STT 변환 -> LLM 분석 -> 데이터베이스 저장이 순차적으로 일어나도록 설계할 때 좋습니다.

---

---

## 1. 싱글톤 패턴(Singleton Pattern)

**싱글톤 패턴(Singleton Pattern)** 은 애플리케이션 전체에서 특정 클래스의 인스턴스를 **단 하나만 생성**하도록 보장하고, 어디서든 그 인스턴스에 접근할 수 있는 전역 지점을 제공하는 디자인 패턴입니다.

사용자님이 구축 중인 **AI 모의 면접 시스템**에서 데이터베이스 연결 풀(DB Connection Pool), 설정 관리자, 혹은 로그 기록기와 같이 자원을 공유해야 하는 경우에 필수적으로 사용됩니다.

---

### 1. 싱글톤 패턴의 핵심 개념

* **유일성 보장:** 시스템 내에 객체가 단 하나만 존재해야 합니다.
* **전역 접근:** 생성된 인스턴스를 어디서나 동일하게 참조할 수 있어야 합니다.
* **자원 절약:** 동일한 객체를 반복해서 생성하지 않으므로 메모리와 오버헤드를 줄입니다.

---

### 2. 파이썬에서의 싱글톤 구현 방법

파이썬은 Java나 C++와 달리 생성자를 숨기는 방식이 독특합니다. 주로 `__new__` 매직 메서드를 오버라이딩하거나, 메타클래스(Metaclass)를 사용합니다.

#### ① `__new__` 메서드를 이용한 기본 방식

객체가 생성될 때 호출되는 `__new__`를 제어하여 이미 인스턴스가 있다면 새로 만들지 않고 기존 것을 반환합니다.

**Python**

```python
class Singleton:
    _instance = None  # 인스턴스를 담을 클래스 변수

    def __new__(cls, *args, **kwargs):
        if not cls._instance:
            # 인스턴스가 없을 때만 실제 객체 생성
            cls._instance = super(Singleton, cls).__new__(cls)
        return cls._instance

# 실행 확인
s1 = Singleton()
s2 = Singleton()

print(f"s1 주소: {id(s1)}")
print(f"s2 주소: {id(s2)}")
print(f"두 객체는 같은가? {s1 is s2}") # True
```

#### ② 메타클래스(Metaclass)를 이용한 방식 (고급/추천)

클래스의 동작 자체를 정의하는 메타클래스를 사용하면, 여러 클래스에 싱글톤 기능을 재사용하기 좋습니다.

**Python**

```python
class SingletonMeta(type):
    _instances = {}

    def __call__(cls, *args, **kwargs):
        if cls not in cls._instances:
            # 클래스별로 인스턴스가 없으면 생성하여 저장
            instance = super().__call__(*args, **kwargs)
            cls._instances[cls] = instance
        return cls._instances[cls]

class DatabaseConnector(metaclass=SingletonMeta):
    def __init__(self):
        self.connection = "Oracle DB Connected"

# 여러 번 호출해도 단 하나의 연결만 유지됨
db1 = DatabaseConnector()
db2 = DatabaseConnector()

print(db1 is db2)  # True
```

---

### 3. 싱글톤 패턴의 장단점

| **장점**                                                   | **단점**                                                                     |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| **메모리 효율:**인스턴스를 하나만 유지하여 메모리 낭비를 방지함. | **결합도 증가:**전역 상태를 가지므로 객체 간 의존성이 높아짐.                      |
| **데이터 공유:**설정값 등을 여러 모듈에서 쉽게 공유 가능.        | **테스트 어려움:**인스턴스 상태가 유지되어 단위 테스트 시 초기화가 복잡함.         |
| **속도:**두 번째 호출부터는 생성 과정이 생략되어 빠름.           | **멀티스레딩 이슈:**동시 접근 시 인스턴스가 두 개 생길 수 있어 Lock 처리가 필요함. |

---

### 4. AI 면접 시스템에서의 실전 활용 예시

서버 C의 **DB 연결**이나 서버 B의 **무거운 AI 모델 관리자**를 싱글톤으로 설계하면 자원 낭비를 막을 수 있습니다.

**Python**

```python
class AIModelManager(metaclass=SingletonMeta):
    def __init__(self):
        print("로딩 중... 6GB VRAM 점유 시작")
        self.model = "Llama-3.1 Loaded"

# 서버 B 내의 여러 서비스가 AIModelManager를 불러도 
# 모델은 처음에 딱 한 번만 메모리에 올라갑니다.
worker1 = AIModelManager()
worker2 = AIModelManager() # 이미 로드된 인스턴스를 즉시 반환
```

---

### 💡 주의할 점 (Thread-Safe)

멀티스레드 환경에서는 여러 스레드가 동시에 `if not cls._instance` 체크를 통과하여 인스턴스가 두 개 생길 위험이 있습니다. 이 경우 파이썬의 `threading.Lock()`을 사용하여 임계 영역을 보호해야 합니다.

---

---

## 2. 팩토리 메서드 (Factory Method) 패턴

**팩토리 메서드(Factory Method) 패턴**은 객체 생성 로직을 자식 클래스에 위임하여, **"어떤 클래스의 인스턴스를 생성할지"**를 서브클래스가 결정하게 만드는 생성 디자인 패턴입니다.

객체를 직접 생성(`new` 또는 `Class()`)하지 않고 별도의 공장(Factory) 인터페이스를 통해 생성함으로써, 시스템의 유연성을 높이고 결합도를 낮추는 것이 핵심입니다.

---

### 1. 팩토리 메서드 패턴의 핵심 구조

* **Product (제품):** 생성될 객체의 공통 인터페이스입니다.
* **Concrete Product (구체적인 제품):** 인터페이스를 실제로 구현한 구체적인 클래스입니다.
* **Creator (추상 생성자):** 팩토리 메서드를 선언하는 추상 클래스입니다.
* **Concrete Creator (구체적 생성자):** 팩토리 메서드를 오버라이딩하여 특정 구체적인 제품을 실제로 생성합니다.

---

### 2. 왜 사용하는가? (장점)

1. **결합도 감소:** 클라이언트는 구체적인 클래스 이름 대신 인터페이스만 알면 됩니다.
2. **확장성 (Open-Closed Principle):** 기존 코드를 수정하지 않고 새로운 제품 클래스를 추가할 수 있습니다.
3. **단일 책임 원칙 (Single Responsibility Principle):** 객체 생성 코드를 한곳(팩토리)으로 모아 관리하기 쉽습니다.

---

### 3. 실전 활용 사례: AI 모의 면접 질문 생성기

현재 개발 중이신 시스템에서 **지원 직무(IT, 마케팅, 경영 등)**에 따라 서로 다른 질문 추출 로직이나 모델을 적용해야 할 때 매우 유용합니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# 1. Product 인터페이스 (면접 질문 추출기)
class QuestionExtractor(ABC):
    @abstractmethod
    def extract(self):
        pass

# 2. Concrete Product (IT 직무용 질문 추출기)
class ITQuestionExtractor(QuestionExtractor):
    def extract(self):
        return "자바의 가비지 컬렉션 원리에 대해 설명해주세요."

# 2. Concrete Product (마케팅 직무용 질문 추출기)
class MarketingQuestionExtractor(QuestionExtractor):
    def extract(self):
        return "최근 가장 인상 깊었던 마케팅 캠페인은 무엇인가요?"

# 3. Creator (추상 생성자 - 공장)
class InterviewFactory(ABC):
    @abstractmethod
    def create_extractor(self) -> QuestionExtractor:
        """팩토리 메서드: 실제 객체 생성은 서브클래스에 맡깁니다."""
        pass

    def start_interview(self):
        """객체 생성 외의 공통 로직을 포함할 수 있습니다."""
        extractor = self.create_extractor()
        print(f"질문 추출 중... 결과: {extractor.extract()}")

# 4. Concrete Creator (IT 직무 공장)
class ITInterviewFactory(InterviewFactory):
    def create_extractor(self) -> QuestionExtractor:
        return ITQuestionExtractor()

# 4. Concrete Creator (마케팅 직무 공장)
class MarketingInterviewFactory(InterviewFactory):
    def create_extractor(self) -> QuestionExtractor:
        return MarketingQuestionExtractor()

# --- 클라이언트 사용 코드 ---
def client_code(factory: InterviewFactory):
    factory.start_interview()

print("--- IT 면접 시작 ---")
client_code(ITInterviewFactory())

print("\n--- 마케팅 면접 시작 ---")
client_code(MarketingInterviewFactory())
```

---

### 4. 팩토리 메서드 vs 싱글톤

* **싱글톤:** "이 객체는 딱 하나만 있어야 해!" (자원 공유 및 관리)
* **팩토리 메서드:** "어떤 객체를 만들지는 상황(서브클래스)에 따라 정할게!" (객체 생성의 추상화)

---

### 💡 현재 프로젝트를 위한 팁

서버 B에서 **비디오 분석 모델**을 돌릴 때, 영상의 종류(면접 영상 vs 단순 표정 인식 영상)에 따라 다른 분석 엔진을 로드해야 한다면 이 팩토리 메서드 패턴을 적용해 보세요. 코드 구조가 훨씬 탄탄해질 것입니다.

---

---

## 3. 추상 팩토리 (Abstract Factory) 패턴

**추상 팩토리(Abstract Factory) 패턴**은 연관된 객체들의 군(Family)을 생성하기 위한 인터페이스를 제공하는 생성 패턴입니다.

팩토리 메서드 패턴이 **'하나의 제품'**을 만드는 법을 추상화한다면, 추상 팩토리는 **'관련된 여러 제품들의 세트'**를 일관성 있게 만드는 법을 추상화합니다.

---

### 1. 추상 팩토리 패턴의 핵심 개념

* **Abstract Factory** : 관련 제품 군을 생성하기 위한 작업들을 선언하는 인터페이스입니다.
* **Concrete Factory** : 구체적인 제품 세트를 실제로 생성하는 클래스입니다.
* **Abstract Product** : 제품 군 내부의 개별 제품들에 대한 인터페이스입니다.
* **Concrete Product** : 특정 팩토리에서 생성되는 실제 객체들입니다.
* **Client** : 추상 팩토리와 추상 제품 인터페이스만을 사용하여 일관된 제품 세트를 공급받습니다.

---

### 2. 왜 사용하는가? (장점)

1. **일관성 보장** : 생성되는 제품들이 서로 호환되는지(같은 제품 군인지) 보장할 수 있습니다.
2. **결합도 감소** : 클라이언트는 구체적인 클래스 대신 인터페이스에 의존하므로 코드 수정이 용이합니다.
3. **단일 책임 원칙** : 제품 생성 로직을 한 곳으로 추출하여 관리합니다.

---

### 3. 실전 활용 사례: AI 면접 시스템의 직무별 엔진 세트

현재 프로젝트에서 **IT 직무**와 **영업 직무**에 따라 **[질문 추출기]**와 **[평가 루브릭]**이 세트로 움직여야 한다고 가정해 봅시다. IT 질문을 가져왔는데 영업용 루브릭으로 채점하면 안 되기 때문에 추상 팩토리가 필요합니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# --- 1. Abstract Products (제품 인터페이스들) ---
class QuestionExtractor(ABC):
    @abstractmethod
    def get_question(self): pass

class EvaluationRubric(ABC):
    @abstractmethod
    def get_criteria(self): pass

# --- 2. Concrete Products (IT 직무 제품군) ---
class ITQuestionExtractor(QuestionExtractor):
    def get_question(self): return "자바의 멀티스레딩 원리에 대해 설명하세요."

class ITEvaluationRubric(EvaluationRubric):
    def get_criteria(self): return "기술적 정확성 및 동시성 제어 이해도"

# --- 2. Concrete Products (영업 직무 제품군) ---
class SalesQuestionExtractor(QuestionExtractor):
    def get_question(self): return "거절하는 고객을 설득한 경험이 있나요?"

class SalesEvaluationRubric(EvaluationRubric):
    def get_criteria(self): return "공감 능력 및 커뮤니케이션 설득력"

# --- 3. Abstract Factory (추상 팩토리) ---
class InterviewEngineFactory(ABC):
    @abstractmethod
    def create_extractor(self) -> QuestionExtractor: pass

    @abstractmethod
    def create_rubric(self) -> EvaluationRubric: pass

# --- 4. Concrete Factories (구체적 팩토리들) ---
class ITInterviewFactory(InterviewEngineFactory):
    def create_extractor(self): return ITQuestionExtractor()
    def create_rubric(self): return ITEvaluationRubric()

class SalesInterviewFactory(InterviewEngineFactory):
    def create_extractor(self): return SalesQuestionExtractor()
    def create_rubric(self): return SalesEvaluationRubric()

# --- 5. Client (클라이언트) ---
def run_interview(factory: InterviewEngineFactory):
    extractor = factory.create_extractor()
    rubric = factory.create_rubric()
  
    print(f"질문: {extractor.get_question()}")
    print(f"평가 기준: {rubric.get_criteria()}")

# 실행 테스트
print("### IT 면접 엔진 가동 ###")
run_interview(ITInterviewFactory())

print("\n### 영업 면접 엔진 가동 ###")
run_interview(SalesInterviewFactory())
```

---

### 4. 팩토리 메서드 vs 추상 팩토리

| **구분**   | **팩토리 메서드 (Factory Method)** | **추상 팩토리 (Abstract Factory)**     |
| ---------------- | ---------------------------------------- | -------------------------------------------- |
| **초점**   | **메서드**수준 (하나의 객체 생성)  | **객체(클래스)**수준 (연관된 여러 객체 생성) |
| **추상화** | 상속을 통해 객체 생성 결정               | 객체 구성을 통해 객체 군 생성                |
| **복잡도** | 상대적으로 낮음                          | 높음 (인터페이스가 많아짐)                   |

---

### 💡 프로젝트 적용 팁

서버 B에서 **비디오 분석**을 할 때, `FaceAnalysisEngine`과 `VoiceAnalysisEngine`이 각각 다른 모델을 사용한다면(예: On-premise용 세트 vs Cloud용 세트), 추상 팩토리를 통해 **'분석 엔진 세트'**를 통째로 교체할 수 있습니다.

---

---

## 4. 빌더(Builder) 패턴

**빌더(Builder) 패턴**은 복잡한 객체의 **생성 과정**과 **표현 방법**을 분리하여, 동일한 생성 절차에서 서로 다른 표현 결과를 만들 수 있게 하는 생성 디자인 패턴입니다.

객체를 생성할 때 생성자(Constructor)에 인자가 너무 많아지거나(Telescoping Constructor), 인자의 순서가 헷갈리는 문제를 해결하기 위해 주로 사용됩니다.

---

### 1. 빌더 패턴의 핵심 구조

* **Product** : 생성될 복잡한 객체입니다.
* **Builder** : 객체의 각 부분을 생성하기 위한 추상 인터페이스입니다.
* **Concrete Builder** : Builder 인터페이스를 구현하며, 부품을 실제로 제조하고 조립 상태를 유지합니다.
* **Director** : Builder 인터페이스를 사용하여 객체를 생성하는 순서를 정의합니다. (선택 사항이지만 구조적 설계를 위해 권장됩니다.)

---

### 2. 왜 사용하는가? (장점)

1. **가독성 향상** : 인자가 많은 생성자 대신 메서드 체이닝(Method Chaining)을 통해 직관적으로 객체를 만듭니다.
2. **불변성 보장** : 객체 생성이 완전히 끝난 후에만 인스턴스를 반환하므로, 불완전한 객체가 사용되는 것을 방지합니다.
3. **코드 재사용** : 동일한 생성 공정(Director)을 거치면서도 서로 다른 결과물(Product)을 얻을 수 있습니다.

---

### 3. 실전 활용 사례: AI 면접 결과 리포트 생성기

현재 개발 중이신 시스템에서 **면접 결과 리포트**를 생성할 때, 어떤 리포트는 '점수'만 포함하고, 어떤 리포트는 'AI 피드백'과 '그래프'까지 포함해야 할 때 빌더 패턴이 아주 적합합니다.

#### Python 예제 코드

**Python**

```python
# 1. Product (최종 결과물)
class InterviewReport:
    def __init__(self):
        self.contents = []

    def add(self, content):
        self.contents.append(content)

    def show(self):
        print("--- 면접 결과 리포트 ---")
        print("\n".join(self.contents))

# 2. Builder 인터페이스
from abc import ABC, abstractmethod

class ReportBuilder(ABC):
    @abstractmethod
    def build_scores(self): pass

    @abstractmethod
    def build_feedback(self): pass

    @abstractmethod
    def build_graph(self): pass

    @abstractmethod
    def get_report(self) -> InterviewReport: pass

# 3. Concrete Builder (상세 리포트 빌더)
class DetailedReportBuilder(ReportBuilder):
    def __init__(self):
        self.report = InterviewReport()

    def build_scores(self):
        self.report.add("점수: 95점 (상위 5%)")

    def build_feedback(self):
        self.report.add("피드백: 전반적으로 논리적인 답변이 돋보입니다.")

    def build_graph(self):
        self.report.add("그래프: [역량 분석 방사형 그래프 포함]")

    def get_report(self):
        return self.report

# 4. Director (생성 공정 관리)
class ReportDirector:
    def __init__(self, builder: ReportBuilder):
        self.builder = builder

    def construct_full_report(self):
        self.builder.build_scores()
        self.builder.build_feedback()
        self.builder.build_graph()

    def construct_summary_report(self):
        self.builder.build_scores()
        self.builder.build_feedback()

# --- 클라이언트 사용 코드 ---
builder = DetailedReportBuilder()
director = ReportDirector(builder)

print("### 요약 리포트 생성 ###")
director.construct_summary_report()
summary = builder.get_report()
summary.show()

print("\n### 전체 상세 리포트 생성 ###")
# 새로운 빌더로 다시 생성
full_builder = DetailedReportBuilder()
director = ReportDirector(full_builder)
director.construct_full_report()
full_report = full_builder.get_report()
full_report.show()
```

---

### 4. 생성 패턴들 간의 관계

* **팩토리 메서드** : 어떤 자식 클래스를 만들지 결정하는 데 집중합니다.
* **추상 팩토리** : 관련된 객체들의 세트(군)를 만드는 데 집중합니다.
* **빌더** : **단계별 생성 절차**와 **복잡한 조합**에 집중합니다.

---

### 💡 프로젝트 적용 팁

서버 C에서 **평가 루브릭 DB 설계**를 하실 때, 루브릭의 구성 요소(평가 항목, 가점 기준, 질문 매칭 등)가 복잡하게 얽혀 있다면 `RubricBuilder`를 만들어 보세요. DB 데이터를 객체화할 때 발생할 수 있는 오류를 크게 줄일 수 있습니다.

---

---

## 5. 프로토타입(Prototype) 패턴

**프로토타입(Prototype) 패턴**은 객체를 처음부터 새로 생성하는 대신, **기존에 존재하는 객체를 복사(Clone)하여 새로운 객체를 생성**하는 생성 디자인 패턴입니다.

객체 생성 비용이 매우 크거나(예: DB 조회, 무거운 초기화 작업), 비슷한 상태의 객체가 반복적으로 필요할 때 성능을 최적화하기 위해 사용됩니다.

---

### 1. 프로토타입 패턴의 핵심 개념

* **Prototype (인터페이스):** 자신을 복제하는 메서드(`clone`)를 선언합니다.
* **Concrete Prototype:** `clone` 메서드를 실제로 구현하여 자신의 복사본을 반환합니다.
* **Client:** 객체를 새로 만들지 않고, 프로토타입 객체에 복제를 요청합니다.

---

### 2. 왜 사용하는가? (장점)

1. **성능 향상:** 복잡하고 무거운 초기화 과정을 생략하고 메모리 상의 데이터를 복사하므로 속도가 빠릅니다.
2. **객체 생성의 추상화:** 클라이언트는 객체의 구체적인 클래스 형식을 몰라도 복제본을 얻을 수 있습니다.
3. **상태 보존:** 특정 시점의 객체 상태를 그대로 복사하여 다른 작업을 수행하기 용이합니다.

---

### 3. 실전 활용 사례: AI 면접 평가 루브릭 설정

사용자님이 설계 중인 **평가 루브릭(Rubric)** 시스템에서, '기본 공통 루브릭'을 먼저 로드한 뒤 직무별로 조금씩만 수정해서 사용해야 한다면 매번 DB에서 읽어오는 것보다 프로토타입 패턴이 훨씬 효율적입니다.

#### Python 예제 코드

파이썬에서는 별도의 인터페이스 없이 `copy` 모듈의 `deepcopy`를 사용하여 아주 쉽게 구현할 수 있습니다.

**Python**

```python
import copy

# 1. Prototype 클래스
class InterviewRubric:
    def __init__(self, job_category, criteria):
        # 이 초기화 과정이 매우 무겁다고 가정 (예: 대규모 DB 조회 및 분석)
        self.job_category = job_category
        self.criteria = criteria
        print(f"--- [{job_category}] 루브릭 초기 로딩 완료 (비용 발생) ---")

    def clone(self):
        # deepcopy를 사용하여 내부 리스트까지 완벽하게 복제
        return copy.deepcopy(self)

    def __str__(self):
        return f"직무: {self.job_category}, 평가 항목: {self.criteria}"

# 2. 실행 및 테스트
# 기본 공통 루브릭 생성 (딱 한 번만 무거운 작업 수행)
base_rubric = InterviewRubric("Common", ["논리성", "태도", "언어전달력"])

# IT 직무용으로 복제 후 수정
it_rubric = base_rubric.clone()
it_rubric.job_category = "IT"
it_rubric.criteria.append("기술적 이해도")

# 영업 직무용으로 복제 후 수정
sales_rubric = base_rubric.clone()
sales_rubric.job_category = "Sales"
sales_rubric.criteria.append("설득력")

print("\n--- 결과 확인 ---")
print(f"원본: {base_rubric}")
print(f"복제본 1: {it_rubric}")
print(f"복제본 2: {sales_rubric}")
```

---

### 4. 주의사항: 얕은 복사(Shallow) vs 깊은 복사(Deep)

프로토타입 패턴을 구현할 때 가장 주의해야 할 점은 **복사의 깊이**입니다.

* **얕은 복사 (`copy.copy`):** 객체 자체는 복사하지만, 내부의 리스트나 객체들은 참조 주소만 복사합니다. 원본 리스트를 수정하면 복제본도 같이 변하는 대참사가 일어날 수 있습니다.
* **깊은 복사 (`copy.deepcopy`):** 객체 내부의 모든 계층을 새로 생성합니다. 프로토타입 패턴에서는 보통 **깊은 복사**를 사용합니다.

---

### 💡 프로젝트 적용 팁

서버 C에서 **평가 루브릭 DB** 데이터를 메모리에 캐싱해둘 때, 각 면접 세션마다 동일한 루브릭 객체를 공유하면 한 면접자의 점수가 다른 면접자에게 영향을 줄 수 있습니다. 이때 `clone()`을 통해 각 면접 세션마다 독립적인 루브릭 인스턴스를 할당해 주면 데이터 오염을 완벽히 방지할 수 있습니다.

---

---

## 6. 어댑터(Adapter) 패턴

**어댑터(Adapter) 패턴**은 서로 일치하지 않는 인터페이스를 가진 클래스들이 함께 작동할 수 있도록 중간에서 연결해 주는 구조 디자인 패턴입니다.

현실 세계의 '돼지코'라고 불리는 **전원 어댑터**를 생각하면 쉽습니다. 한국의 220V 플러그(Client)를 일본의 110V 콘센트(Service)에 꽂으려면 중간에 어댑터가 필요한 것과 같은 원리입니다.

---

### 1. 어댑터 패턴의 핵심 구조

어댑터 패턴은 기존 코드를 수정하지 않고도 새로운 인터페이스나 외부 라이브러리를 적용하고 싶을 때 사용합니다.

* **Target (타겟):** 클라이언트가 직접적으로 사용하려는 표준 인터페이스입니다.
* **Client (클라이언트):** Target 인터페이스를 통해 비즈니스 로직을 수행하는 주체입니다.
* **Adaptee (어댑티):** 사용하고 싶지만 인터페이스가 호환되지 않는 기존 클래스나 외부 라이브러리입니다.
* **Adapter (어댑터):** 클라이언트의 요청을 Adaptee가 이해할 수 있는 방식으로 변환하여 전달하는 중간 클래스입니다.

---

### 2. 왜 사용하는가? (장점)

1. **기존 코드 보존:** 이미 검증된 기존 클래스(Adaptee)를 수정하지 않고도 새로운 규격에 맞게 재사용할 수 있습니다.
2. **유연한 통합:** 타사 라이브러리나 레거시 시스템의 인터페이스가 바뀌어도 어댑터만 수정하면 되므로 유지보수가 쉽습니다.
3. **단일 책임 원칙:** 데이터 변환 로직을 비즈니스 로직에서 분리하여 어댑터에 집중시킵니다.

---

### 3. 실전 활용 사례: AI 면접 시스템의 STT 엔진 통합

사용자님이 구축 중인 시스템에서, 처음에는 **Google STT**를 사용하다가 나중에 **Naver CLOVA STT**로 변경하거나 병행해야 하는 상황을 가정해 보겠습니다. 두 서비스의 메서드 이름과 형식이 다르더라도 어댑터를 쓰면 클라이언트 코드를 고칠 필요가 없습니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# 1. Target: 우리 시스템의 표준 STT 인터페이스
class SpeechToText(ABC):
    @abstractmethod
    def transcribe(self, audio_data):
        pass

# 2. Adaptee: 호환되지 않는 외부 라이브러리 (Google)
class GoogleSTT:
    def recognize_speech(self, data):
        return f"Google 결과: {data[:10]}..."

# 3. Adaptee: 호환되지 않는 또 다른 라이브러리 (Naver)
class NaverCLOVA:
    def speech_to_text_converter(self, audio):
        return f"Naver 결과: {audio[:10]}..."

# 4. Adapter: Google용 어댑터
class GoogleAdapter(SpeechToText):
    def __init__(self, adaptee: GoogleSTT):
        self.adaptee = adaptee

    def transcribe(self, audio_data):
        # 인터페이스 이름을 맞추고 내부 로직 호출
        return self.adaptee.recognize_speech(audio_data)

# 4. Adapter: Naver용 어댑터
class NaverAdapter(SpeechToText):
    def __init__(self, adaptee: NaverCLOVA):
        self.adaptee = adaptee

    def transcribe(self, audio_data):
        return self.adaptee.speech_to_text_converter(audio_data)

# --- 클라이언트 사용 코드 ---
def process_interview_audio(stt_engine: SpeechToText, audio):
    # 클라이언트는 어떤 엔진인지 몰라도 transcribe()만 부르면 됨
    result = stt_engine.transcribe(audio)
    print(result)

audio_sample = "안녕하세요, 지원자 홍길동입니다."

# 필요에 따라 엔진을 갈아 끼우며 사용
process_interview_audio(GoogleAdapter(GoogleSTT()), audio_sample)
process_interview_audio(NaverAdapter(NaverCLOVA()), audio_sample)
```

---

### 4. 어댑터 vs 프록시 vs 데코레이터

구조가 비슷해 보이지만 목적이 명확히 다릅니다.

| **패턴**       | **목적**            | **특징**                                 |
| -------------------- | ------------------------- | ---------------------------------------------- |
| **어댑터**     | **인터페이스 변환** | 호환되지 않는 두 인터페이스를 연결함           |
| **프록시**     | **접근 제어**       | 실제 객체 호출 전 권한 체크나 지연 로딩 수행   |
| **데코레이터** | **기능 확장**       | 객체의 코드를 바꾸지 않고 새로운 행동을 추가함 |

---

### 💡 프로젝트 적용 팁

서버 C에서 **PostgreSQL(pgvector)**과 **Oracle**을 동시에 다루실 때, 각 DB 드라이버의 API가 미세하게 다를 수 있습니다. 이때 `DatabaseAdapter`를 만들어서 공통의 `execute_query()` 메서드로 통일하면, 상위 비즈니스 로직에서 DB 종류에 상관없이 쿼리를 날릴 수 있어 매우 편리해집니다.

---

---

## 7. 프록시 (Proxy) 패턴

**프록시(Proxy) 패턴**은 실제 객체에 대한 접근을 제어하거나 부가 기능을 수행하기 위해 '대리인(Proxy)'을 내세우는 구조 디자인 패턴입니다.

사용자가 실제 객체를 직접 호출하는 대신 프록시를 거치게 함으로써, 객체 생성 시점을 늦추거나(지연 로딩), 권한을 확인하거나, 네트워크 통신을 숨기는 등 다양한 작업을 수행할 수 있습니다.

---

### 1. 프록시 패턴의 핵심 구조

프록시 패턴의 가장 큰 특징은 **프록시와 실제 객체가 동일한 인터페이스를 구현**한다는 점입니다. 클라이언트는 자신이 상대하는 것이 실제 객체인지 프록시인지 알 필요가 없습니다.

* **Subject (인터페이스):** 실제 객체와 프록시가 공통으로 구현해야 하는 인터페이스입니다.
* **RealSubject (실제 객체):** 실제 비즈니스 로직을 수행하는 무거운 객체입니다.
* **Proxy (대리인):** 실제 객체에 대한 참조를 가지며, 요청을 전달하기 전후에 제어 로직을 실행합니다.

---

### 2. 주요 유형 및 용도

1. **가상 프록시 (Virtual Proxy):** 실제 객체의 생성 비용이 많이 들 때, 정말 필요한 시점까지 생성을 미룹니다. (지연 로딩)
2. **보호 프록시 (Protection Proxy):** 클라이언트의 권한에 따라 실제 객체의 메서드 호출을 제한합니다.
3. **원격 프록시 (Remote Proxy):** 다른 주소 공간(서버)에 있는 객체를 로컬에 있는 것처럼 보이게 합니다.
4. **로깅/캐싱 프록시:** 요청에 대한 로그를 남기거나, 이전 결과를 캐싱하여 성능을 높입니다.

---

### 3. 실전 활용 사례: AI 모델 지연 로딩 (Virtual Proxy)

사용자님이 구축 중인 **AI 모의 면접 시스템** 서버 B에서 수 기가의 메모리를 점유하는 **Heavy AI Model**을 다룬다고 가정해 보겠습니다. 프로그램 시작 시 모든 모델을 로드하면 메모리가 부족할 수 있으므로, 실제 면접이 시작될 때만 모델을 로드하는 프록시를 설계할 수 있습니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod
import time

# 1. Subject: 공통 인터페이스
class AIModel(ABC):
    @abstractmethod
    def predict(self, data):
        pass

# 2. RealSubject: 실제 무거운 AI 모델
class HeavyAIModel(AIModel):
    def __init__(self):
        print("🛠️ 무거운 AI 모델 로딩 중... (약 5초 소요)")
        time.sleep(5)  # 무거운 로딩 과정 시뮬레이션
        print("✅ 모델 로딩 완료!")

    def predict(self, data):
        return f"🤖 AI 분석 결과: '{data}'는 매우 훌륭한 답변입니다."

# 3. Proxy: 대리인 (가상 프록시)
class AIModelProxy(AIModel):
    def __init__(self):
        self._real_model = None  # 실제 모델은 아직 생성하지 않음

    def predict(self, data):
        # 처음 호출되는 시점에만 실제 객체를 생성 (Lazy Loading)
        if self._real_model is None:
            self._real_model = HeavyAIModel()
    
        print("🔍 프록시: 요청을 분석하고 모델에 전달합니다.")
        return self._real_model.predict(data)

# --- 클라이언트 사용 코드 ---
# 프록시를 생성할 때는 모델이 로드되지 않아 매우 빠릅니다.
proxy = AIModelProxy()
print("🚀 프록시 객체가 준비되었습니다. (모델은 아직 로드되지 않음)")

print("\n--- 첫 번째 질문 분석 시도 ---")
# 이때 비로소 HeavyAIModel이 생성됩니다.
print(proxy.predict("제 장점은 성실함입니다."))

print("\n--- 두 번째 질문 분석 시도 ---")
# 이미 생성된 모델을 사용하므로 즉시 결과가 나옵니다.
print(proxy.predict("협업 능력이 뛰어납니다."))
```

---

### 4. 프록시 패턴 vs 어댑터 vs 데코레이터

| **패턴**       | **핵심 의도**       | **특징**                                      |
| -------------------- | ------------------------- | --------------------------------------------------- |
| **프록시**     | **접근 제어**       | 인터페이스를 유지하면서 실제 객체로의 접근을 관리함 |
| **어댑터**     | **인터페이스 변환** | 호환되지 않는 두 인터페이스를 연결함                |
| **데코레이터** | **기능 확장**       | 실제 객체의 행동을 동적으로 추가하거나 변경함       |

---

### 💡 프로젝트 적용 팁

현재 **서버 C**에서 PostgreSQL과 Oracle을 다루고 계신데, 데이터베이스 연결이 끊겼을 때 자동으로 재연결하거나 쿼리 실행 시간을 기록하는 **Logging/Retry Proxy**를 만들어 보세요. 비즈니스 로직(쿼리 전송)을 수정하지 않고도 시스템의 안정성을 획기적으로 높일 수 있습니다.

---

---

## 8. 데코레이터 (Decorator) 패턴

**데코레이터(Decorator) 패턴**은 객체에 동적으로 새로운 책임(기능)을 추가할 수 있게 허용하는 구조 디자인 패턴입니다.

상속을 통해 기능을 확장하는 대신, **객체를 다른 객체로 감싸는(Wrapping)** 방식을 사용하여 런타임에 유연하게 기능을 조합할 수 있다는 것이 가장 큰 특징입니다.

---

### 1. 데코레이터 패턴의 핵심 구조

데코레이터 패턴은 기본 기능을 수행하는 클래스와 이를 보조하는 장식자 클래스들이 **동일한 인터페이스**를 공유하도록 설계합니다.

* **Component (인터페이스):** 기본 기능을 정의하는 인터페이스입니다.
* **ConcreteComponent:** 인터페이스를 구현한 실제 기본 객체입니다. (예: 아메리카노)
* **Decorator:** Component를 참조하며, 인터페이스를 똑같이 구현하는 추상 클래스입니다.
* **ConcreteDecorator:** 실제로 기능을 추가하는 클래스입니다. (예: 샷 추가, 휘핑크림 추가)

---

### 2. 왜 사용하는가? (장점)

1. **유연한 확장:** 클래스 상속은 정적이지만, 데코레이터는 런타임에 객체를 조합하여 기능을 확장할 수 있습니다.
2. **단일 책임 원칙:** 다양한 기능을 별도의 클래스로 분리하여 관리할 수 있습니다.
3. **조합의 폭발 해결:** 상속으로 모든 경우의 수(샷 추가 아메리카노, 시럽 추가 아메리카노 등)를 클래스로 만들면 클래스 수가 너무 많아지지만, 데코레이터는 몇 개의 클래스 조합으로 이를 해결합니다.

---

### 3. 실전 활용 사례: AI 면접 시스템의 결과 처리

사용자님이 구축 중인 시스템에서 **면접 결과 텍스트**를 처리할 때, 단순히 텍스트만 보여줄 수도 있지만 상황에 따라 '욕설 필터링', '맞춤법 검사', 'HTML 태그 추가' 등을 선택적으로 적용해야 할 때 유용합니다.

#### Python 예제 코드

파이썬의 함수 데코레이터(`@decorator`)와 혼동될 수 있으나, 여기서는 **객체지향 디자인 패턴**으로서의 데코레이터를 구현해 보겠습니다.

**Python**

```python
from abc import ABC, abstractmethod

# 1. Component: 공통 인터페이스
class InterviewResult(ABC):
    @abstractmethod
    def get_content(self) -> str:
        pass

# 2. ConcreteComponent: 기본 결과 객체
class PlainResult(InterviewResult):
    def __init__(self, text):
        self._text = text

    def get_content(self) -> str:
        return self._text

# 3. Decorator: 장식자 추상 클래스
class ResultDecorator(InterviewResult):
    def __init__(self, result: InterviewResult):
        self._result = result

    @abstractmethod
    def get_content(self) -> str:
        pass

# 4. ConcreteDecorator A: 욕설 필터링
class ProfanityFilter(ResultDecorator):
    def get_content(self) -> str:
        content = self._result.get_content()
        # 단순 예시로 특정 단어 치환
        return content.replace("바보", "**")

# 4. ConcreteDecorator B: HTML 태그 추가
class HtmlDecorator(ResultDecorator):
    def get_content(self) -> str:
        return f"<div>{self._result.get_content()}</div>"

# --- 클라이언트 사용 코드 ---
# 기본 결과 생성
result = PlainResult("저는 바보가 아닙니다. 열심히 하겠습니다.")

# 필터링 추가
filtered_result = ProfanityFilter(result)

# HTML 태그까지 추가 (중첩 래핑)
final_result = HtmlDecorator(filtered_result)

print("원본:", result.get_content())
print("최종 장식 결과:", final_result.get_content())
```

---

### 4. 프록시(Proxy) 패턴 vs 데코레이터(Decorator)

두 패턴은 구조적으로 매우 유사(객체를 감싸는 방식)하지만 **의도**에서 차이가 납니다.

| **패턴**       | **핵심 의도** | **특징**                              |
| -------------------- | ------------------- | ------------------------------------------- |
| **데코레이터** | **기능 추가** | 객체에 새로운 행동을 덧붙여 기능을 확장함   |
| **프록시**     | **접근 제어** | 실제 객체에 대한 호출을 관리하거나 지연시킴 |

---

### 💡 프로젝트 적용 팁

현재 **서버 C**에서 평가 루브릭 데이터를 가져올 때, `CacheDecorator`를 만들어 보세요. 기본 DB 조회 객체를 감싸서, 한 번 조회한 데이터는 메모리에 들고 있다가 반환하게 만들면 비즈니스 로직을 수정하지 않고도 성능을 비약적으로 높일 수 있습니다.

---

---

## 9. 파사드 (Facade) 패턴

**파사드(Facade) 패턴**은 복잡한 서브시스템(여러 개의 클래스나 라이브러리)에 대해 **단순화된 하나의 인터페이스**를 제공하는 구조 디자인 패턴입니다.

'Facade'는 건물의 **'정면'**이라는 뜻입니다. 건물 내부의 복잡한 배선이나 배관을 알 필요 없이 정문(Facade)을 통해 건물에 들어가듯, 복잡한 내부 로직을 캡슐화하여 사용자가 사용하기 편하게 만들어주는 것이 핵심입니다.

---

### 1. 파사드 패턴의 핵심 구조

사용자가 시스템의 수많은 세부 클래스를 직접 제어하는 대신, 모든 요청을 '파사드'라는 단일 창구로 보내 처리하게 합니다.

* **Facade (파사드):** 서브시스템의 복잡한 기능을 단순하게 묶어서 클라이언트에게 제공합니다.
* **Subsystems (서브시스템):** 실제 비즈니스 로직을 수행하는 수많은 클래스들입니다. 파사드의 존재를 모르며 서로 독립적으로 동작합니다.
* **Client (클라이언트):** 서브시스템을 직접 호출하는 대신 파사드 인터페이스만 사용합니다.

---

### 2. 왜 사용하는가? (장점)

1. **낮은 결합도:** 클라이언트가 서브시스템의 구체적인 클래스들을 알 필요가 없으므로, 내부 코드가 바뀌어도 클라이언트는 영향을 받지 않습니다.
2. **사용 편의성:** 복잡한 메서드 호출 순서나 설정을 파사드 내부에서 처리해주므로 사용법이 매우 간단해집니다.
3. **계층화 구조:** 시스템이 너무 거대해질 때 계층별로 파사드를 두어 구조를 깔끔하게 유지할 수 있습니다.

---

### 3. 실전 활용 사례: AI 면접 시스템의 '면접 시작' 프로세스

사용자님이 구축 중인 시스템에서 **'면접 시작'** 버튼 하나를 누르면 내부적으로는 [카메라 체크], [마이크 활성화], [STT 엔진 초기화], [질문 추출], [데이터베이스 세션 연결] 등 수많은 작업이 동시에 일어나야 합니다. 이를 파사드 패턴으로 묶어보겠습니다.

#### Python 예제 코드

**Python**

```python
# --- 서브시스템들 (Subsystems) ---
class Camera:
    def turn_on(self): print("📹 카메라를 활성화합니다.")

class Microphone:
    def connect(self): print("🎤 마이크 연결을 확인합니다.")

class STTEngine:
    def initialize(self): print("📝 STT 엔진 초기화 중...")

class Database:
    def connect(self): print("🗄️ PostgreSQL 서버에 연결합니다.")

class QuestionLoader:
    def load_job_questions(self, job): 
        print(f"❓ {job} 직무 질문 리스트를 로드합니다.")

# --- 파사드 (Facade) ---
class InterviewFacade:
    def __init__(self):
        self.camera = Camera()
        self.mic = Microphone()
        self.stt = STTEngine()
        self.db = Database()
        self.loader = QuestionLoader()

    def prepare_interview(self, job_title):
        print(f"=== {job_title} 면접 환경 구성을 시작합니다 ===")
        self.db.connect()
        self.camera.turn_on()
        self.mic.connect()
        self.stt.initialize()
        self.loader.load_job_questions(job_title)
        print("✅ 모든 준비가 완료되었습니다. 면접을 시작하세요.")

# --- 클라이언트 사용 코드 ---
# 클라이언트는 복잡한 내부 로직을 몰라도 파사드 하나면 충분합니다.
interview_system = InterviewFacade()
interview_system.prepare_interview("백엔드 개발자")
```

---

### 4. 파사드 패턴 vs 어댑터 패턴

두 패턴은 모두 '중계자' 역할을 하지만 목적이 다릅니다.

| **패턴**             | **목적**              | **특징**                             |
| -------------------------- | --------------------------- | ------------------------------------------ |
| **파사드 (Facade)**  | **인터페이스 단순화** | 여러 개의 클래스를 묶어서 쓰기 편하게 만듦 |
| **어댑터 (Adapter)** | **인터페이스 변환**   | 호환되지 않는 인터페이스를 호환되게 맞춤   |

---

### 💡 프로젝트 적용 팁

현재 **서버 B**에서 여러 AI 모델(Ollama, Whisper 등)을 연동하고 계신데, 이를 개별적으로 호출하기보다 `AIInferenceFacade` 클래스를 만들어 보세요. `get_interview_analysis(audio_file)` 같은 단일 메서드 호출로 STT 변환부터 LLM 분석까지 한 번에 처리하도록 설계하면 메인 로직이 훨씬 간결해집니다.

---

---

## 10. 브릿지 (Bridge) 패턴

**브릿지(Bridge) 패턴**은 큰 클래스나 밀접하게 관련된 클래스들의 집합을 두 개의 개별 계층 구조인 **추상화(Abstraction)**와 **구현(Implementation)**으로 나눈 후, 이들을 독립적으로 확장할 수 있게 연결하는 구조 디자인 패턴입니다.

한마디로 요약하면 **"기능의 계층"**과 **"구현의 계층"**을 분리하여 서로 간의 결합도를 낮추는 패턴입니다.

---

### 1. 브릿지 패턴의 핵심 구조

브릿지 패턴은 상속(Inheritance) 대신 **합성(Composition)**을 사용하여 복잡성을 해결합니다.

* **Abstraction (추상화 계층):** 상위 수준의 제어 로직을 담고 있습니다. 구현 객체에 대한 참조를 가지고 있으며 실제 작업은 구현 객체에 위임합니다.
* **Refined Abstraction:** 추상화 계층을 상속받아 기능을 확장한 클래스입니다.
* **Implementor (구현 인터페이스):** 모든 구체적인 구현 클래스들을 위한 공통 인터페이스입니다.
* **Concrete Implementor:** 인터페이스를 실제로 구현한 구체적인 플랫폼별 코드입니다.

---

### 2. 왜 사용하는가? (장점)

1. **독립적 확장:** 기능(새로운 면접 방식 추가)과 구현(새로운 AI 엔진 도입)을 서로 방해받지 않고 독립적으로 확장할 수 있습니다.
2. **런타임 바인딩:** 상속과 달리 실행 중에 구현 객체를 교체할 수 있습니다.
3. **복잡성 감소:** 계층을 분리함으로써 클래스 폭발(Class Explosion) 현상을 막을 수 있습니다.

---

### 3. 실전 활용 사례: AI 면접 시스템의 서비스와 AI 모델 연동

사용자님이 구축 중인 시스템에서 **'실시간 면접'**과 **'녹화 면접'**이라는 서비스가 있고, 이를 분석하는 엔진으로 **'Ollama(온프레미스)'**와 **'Gemini(클라우드)'**가 있다고 가정해 봅시다. 브릿지 패턴이 없다면 모든 조합(실시간-Ollama, 녹화-Gemini 등)에 대해 클래스를 만들어야 하지만, 브릿지를 쓰면 간단해집니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# --- 1. Implementor: AI 모델 인터페이스 (구현 계층) ---
class AIModel(ABC):
    @abstractmethod
    def analyze_text(self, text):
        pass

# --- 2. Concrete Implementor: 구체적인 AI 엔진들 ---
class OllamaModel(AIModel):
    def analyze_text(self, text):
        return f"Ollama(Local) 분석 결과: '{text[:10]}...'에 대한 로컬 분석 수행"

class GeminiModel(AIModel):
    def analyze_text(self, text):
        return f"Gemini(Cloud) 분석 결과: '{text[:10]}...'에 대한 클라우드 분석 수행"

# --- 3. Abstraction: 면접 서비스 (기능 계층) ---
class InterviewService:
    def __init__(self, model: AIModel):
        self.model = model  # 이것이 브릿지(Bridge) 역할을 합니다.

    def process_interview(self, content):
        print("서비스가 분석을 요청합니다...")
        return self.model.analyze_text(content)

# --- 4. Refined Abstraction: 확장된 서비스 기능 ---
class RealTimeInterview(InterviewService):
    def process_interview(self, content):
        print("[실시간 모드] 데이터 스트리밍 분석 중...")
        return super().process_interview(content)

class RecordedInterview(InterviewService):
    def process_interview(self, content):
        print("[녹화 모드] 저장된 영상 파일 분석 중...")
        return super().process_interview(content)

# --- 클라이언트 사용 코드 ---
# 런타임에 어떤 서비스에 어떤 모델을 쓸지 자유롭게 조합 가능
ollama = OllamaModel()
gemini = GeminiModel()

# 1. 녹화 면접을 로컬 Ollama 모델로 분석
interview_a = RecordedInterview(ollama)
print(interview_a.process_interview("지원자의 답변 텍스트 데이터..."))

print("-" * 30)

# 2. 실시간 면접을 클라우드 Gemini 모델로 분석
interview_b = RealTimeInterview(gemini)
print(interview_b.process_interview("실시간 음성 인식 텍스트 데이터..."))
```

---

### 4. 어댑터(Adapter) 패턴 vs 브릿지(Bridge) 패턴

두 패턴은 구조가 유사해 보이지만 의도에서 큰 차이가 있습니다.

| **패턴**   | **목적**      | **특징**                                                         |
| ---------------- | ------------------- | ---------------------------------------------------------------------- |
| **어댑터** | **호환성**    | 이미 존재하는 클래스들이 함께 작동하도록 인터페이스를 맞춤 (사후 처리) |
| **브릿지** | **설계 분리** | 추상화와 구현을 독립적으로 설계하여 복잡성을 예방 (사전 설계)          |

---

### 💡 프로젝트 적용 팁

현재 **서버 C**에서 PostgreSQL과 Oracle을 동시에 관리하고 계신데, 데이터베이스 저장 서비스(기능)와 실제 DB 드라이버(구현)를 브릿지로 연결해 보세요. 나중에 MariaDB 같은 새로운 DB를 추가하거나, 단순 저장 외에 '암호화 저장' 같은 기능을 추가할 때 기존 코드를 거의 건드리지 않고 확장할 수 있습니다.

---

---

## 11. 옵저버 (Observer) 패턴

**옵저버(Observer) 패턴**은 특정 객체의 상태가 변경되었을 때, 그 객체에 의존하는 다른 객체들에게 변경 사항을 자동으로 통지하고 갱신하게 만드는 행위 디자인 패턴입니다.

흔히 **발행-구독(Publish-Subscribe)** 모델이라고도 불리며, 한 곳에서 일어난 이벤트가 여러 곳으로 전파되어야 할 때 핵심적인 역할을 합니다.

---

### 1. 옵저버 패턴의 핵심 구조

이 패턴은 크게 두 가지 주체로 나뉩니다.

* **Subject (주체/발행자):** 관찰 대상입니다. 옵저버들을 등록(`attach`), 제거(`detach`)하고 상태가 변하면 알림(`notify`)을 보냅니다.
* **Observer (관찰자/구독자):** 주체의 상태 변화를 기다리는 객체들입니다. 알림을 받으면 자신의 상태를 업데이트합니다.

---

### 2. 왜 사용하는가? (장점)

1. **느슨한 결합 (Loose Coupling):** 주체는 옵저버가 누구인지, 어떤 일을 하는지 구체적으로 알 필요가 없습니다. 인터페이스만 맞으면 누구든 구독할 수 있습니다.
2. **실시간성:** 상태가 변하는 즉시 관련 객체들에게 알림을 보낼 수 있어 반응형 시스템 구축에 유리합니다.
3. **개방-폐쇄 원칙 (OCP):** 기존 코드를 수정하지 않고도 새로운 옵저버 클래스를 언제든지 추가할 수 있습니다.

---

### 3. 실전 활용 사례: AI 면접 시스템의 '이벤트 알림'

사용자님이 구축 중인 시스템에서 **'AI 면접 분석 완료'**라는 이벤트가 발생했을 때, [DB 저장], [대시보드 업데이트], [사용자에게 결과 알림 메일 전송]이 동시에 일어나야 한다고 가정해 보겠습니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# 1. Observer 인터페이스 (구독자 규격)
class InterviewObserver(ABC):
    @abstractmethod
    def update(self, result_data):
        pass

# 2. Subject (발행자: 면접 분석 엔진)
class InterviewAnalysisEngine:
    def __init__(self):
        self._observers = []
        self._result = None

    def attach(self, observer: InterviewObserver):
        if observer not in self._observers:
            self._observers.append(observer)

    def detach(self, observer: InterviewObserver):
        self._observers.remove(observer)

    def notify(self):
        for observer in self._observers:
            observer.update(self._result)

    def finish_analysis(self, data):
        print(f"🚀 [Engine] 면접 분석이 완료되었습니다: {data}")
        self._result = data
        self.notify() # 등록된 모든 옵저버에게 알림

# 3. Concrete Observers (구체적인 구독자들)
class DatabaseLogger(InterviewObserver):
    def update(self, result_data):
        print(f"💾 [DB] 데이터를 PostgreSQL에 저장합니다: {result_data}")

class DashboardUpdater(InterviewObserver):
    def update(self, result_data):
        print(f"📊 [Dashboard] 실시간 면접 현황판을 갱신합니다: {result_data}")

class EmailNotifier(InterviewObserver):
    def update(self, result_data):
        print(f"📧 [Email] 지원자에게 면접 완료 안내 메일을 발송합니다.")

# --- 클라이언트 사용 코드 ---
engine = InterviewAnalysisEngine()

# 구독자들 등록
db_log = DatabaseLogger()
dashboard = DashboardUpdater()
email = EmailNotifier()

engine.attach(db_log)
engine.attach(dashboard)
engine.attach(email)

# 분석 완료 이벤트 발생 (자동으로 3곳에 전파됨)
engine.finish_analysis("홍길동 지원자 - A등급")

print("-" * 30)

# 특정 구독자 제거 후 다시 실행
engine.detach(email)
engine.finish_analysis("이몽룡 지원자 - B+등급")
```

---

### 4. 주의사항 및 팁

* **알림 순서:** 옵저버 패턴은 알림이 전달되는 순서를 보장하지 않는 경우가 많습니다. 순서가 중요하다면 추가적인 로직이 필요합니다.
* **무한 루프:** 옵저버가 주체의 상태를 다시 변경하고, 그것이 다시 알림을 유발하면 무한 루프에 빠질 수 있으므로 주의해야 합니다.
* **메모리 누수:** 옵저버를 더 이상 사용하지 않을 때는 반드시 `detach`를 해주어야 합니다. 그렇지 않으면 주체가 참조를 계속 가지고 있어 가비지 컬렉션이 되지 않을 수 있습니다.

---

### 💡 프로젝트 적용 팁

현재 **서버 A(메인)**와 **서버 B(AI)**를 연동 중이신데, 서버 B에서 AI 분석이 끝나면 서버 A로 웹훅(Webhook)을 쏘는 구조가 바로 네트워크 환경에서의 옵저버 패턴입니다. 이를 활용해 비동기적으로 데이터를 주고받으면 서버 간의 부하를 줄일 수 있습니다.

---

---

## 12. 전략 (Strategy) 패턴

**전략(Strategy) 패턴**은 특정 알고리즘(또는 로직)을 캡슐화하고, 이를 상황에 따라 자유롭게 교체하여 사용할 수 있게 만드는 행위 디자인 패턴입니다.

사용자님이 진행 중인 **AI 모의 면접 시스템**에서 면접 난이도(쉬움, 어려움)나 직무(IT, 경영)에 따라 질문을 추출하는 **알고리즘만 갈아 끼우고 싶을 때** 가장 효과적인 해결책입니다.

---

### 1. 전략 패턴의 핵심 구조

상속을 통해 기능을 확장하는 대신, **합성(Composition)**을 사용하여 실행 중에 알고리즘을 선택합니다.

* **Strategy (전략 인터페이스):** 모든 지원하는 알고리즘에 대한 공통 인터페이스입니다.
* **Concrete Strategy (구체적인 전략):** 알고리즘을 실제로 구현한 클래스들입니다.
* **Context (컨텍스트):** 전략 객체를 참조하며, 클라이언트가 요청할 때 해당 전략의 알고리즘을 실행합니다.

---

### 2. 왜 사용하는가? (장점)

1. **if-else 지옥 탈출:** 런타임에 조건문 없이도 로직을 유연하게 변경할 수 있습니다.
2. **개방-폐쇄 원칙(OCP):** 새로운 전략이 추가되어도 기존 컨텍스트 코드를 수정할 필요가 없습니다.
3. **코드 재사용:** 동일한 알고리즘을 여러 다른 컨텍스트에서 재사용할 수 있습니다.

---

### 3. 실전 활용 사례: 면접 질문 추천 엔진

면접의 성격에 따라 **'키워드 기반 추출'**과 **'LLM 기반 생성'** 전략을 선택적으로 사용하는 예제입니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# 1. Strategy Interface (전략 인터페이스)
class QuestionStrategy(ABC):
    @abstractmethod
    def get_questions(self, user_data: str):
        pass

# 2. Concrete Strategies (구체적인 전략들)
class KeywordStrategy(QuestionStrategy):
    def get_questions(self, user_data: str):
        return f"['{user_data}' 키워드 기반] 프로젝트에서 본인의 역할은 무엇이었나요?"

class LLMGenerationStrategy(QuestionStrategy):
    def get_questions(self, user_data: str):
        return f"['{user_data}' 분석 기반] 딥러닝 모델 최적화 경험에 대해 구체적으로 설명해주세요."

# 3. Context (컨텍스트: 질문 생성기)
class InterviewBot:
    def __init__(self, strategy: QuestionStrategy):
        self._strategy = strategy # 전략을 주입받음

    def set_strategy(self, strategy: QuestionStrategy):
        """실행 중에 전략을 바꿀 수 있습니다."""
        self._strategy = strategy

    def generate(self, data):
        # 실제 로직은 전략 객체에 위임
        print("AI가 질문을 생성 중입니다...")
        return self._strategy.get_questions(data)

# --- 클라이언트 사용 코드 ---

# 처음에는 가벼운 키워드 기반 전략 사용
bot = InterviewBot(KeywordStrategy())
print(bot.generate("Python 개발자"))

print("\n--- 전략 교체 (더 정교한 분석 필요 시) ---")

# 런타임에 LLM 기반 생성 전략으로 교체
bot.set_strategy(LLMGenerationStrategy())
print(bot.generate("Python 개발자"))
```

---

### 4. 전략 패턴 vs 상태(State) 패턴

두 패턴은 구조적으로 매우 비슷하지만 **의도**가 다릅니다.

| **패턴**            | **핵심 의도**                  | **특징**                                |
| ------------------------- | ------------------------------------ | --------------------------------------------- |
| **전략 (Strategy)** | **알고리즘 교체**              | 클라이언트가 어떤 알고리즘을 쓸지 직접 선택함 |
| **상태 (State)**    | **상태 변화에 따른 행동 변경** | 객체 내부 상태에 따라 자동으로 행동이 바뀜    |

---

### 💡 프로젝트 적용 팁

현재 **서버 B**에서 여러 AI 모델을 다루고 계신데, 데이터의 크기나 사용자 플랜에 따라 **전략 패턴**을 적용해 보세요.

* **텍스트 양이 적을 때:** `LightModelStrategy` (Ollama 등 로컬 모델)
* **텍스트 양이 많거나 고성능이 필요할 때:** `HeavyModelStrategy` (Gemini API 등)
  이렇게 설계하면 서버 부하를 효율적으로 분산하고 비용도 최적화할 수 있습니다.

---

---

## 13. 상태 (State) 패턴

**상태(State) 패턴**은 객체의 내부 상태가 바뀜에 따라 **객체의 행동을 변경**할 수 있게 해주는 행위 디자인 패턴입니다. 객체는 마치 자신의 클래스를 바꾸는 것처럼 보일 정도로 상태에 따라 완전히 다른 로직을 실행하게 됩니다.

사용자님이 개발 중인 **AI 모의 면접 시스템**에서 면접의 진행 단계( **대기 → 진행 → 평가 → 종료** )에 따라 시스템이 반응하는 방식을 설계할 때 가장 효율적인 패턴입니다.

---

### 1. 상태 패턴의 핵심 구조

상태 패턴의 핵심은 `if-else`나 `switch` 문으로 상태를 분기하는 대신, **상태 자체를 객체화**하여 전략적으로 교체하는 것입니다.

* **Context (컨텍스트):** 현재 상태 객체를 보관하며, 클라이언트의 요청을 현재 상태 객체에 전달합니다.
* **State (상태 인터페이스):** 각 상태에서 수행해야 할 행동들을 정의합니다.
* **Concrete State (구체적인 상태):** 각 상태의 실제 행동을 구현하며, 다음 상태로 전환(Transition)하는 로직을 포함하기도 합니다.

---

### 2. 왜 사용하는가? (장점)

1. **조건문 제거:** 복잡한 `if (state == READY) ... else if (state == RUNNING)` 지옥에서 벗어나 코드가 깔끔해집니다.
2. **단일 책임 원칙(SRP):** 특정 상태와 관련된 로직을 하나의 클래스로 응집시킬 수 있습니다.
3. **상태 전이 가시성:** 상태가 어떻게 변하는지(Transition) 흐름을 명확하게 파악할 수 있습니다.

---

### 3. 실전 활용 사례: AI 면접 세션 관리

면접 시스템의 라이프사이클을 상태 패턴으로 구현한 예제입니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# 1. State Interface (상태 인터페이스)
class InterviewState(ABC):
    @abstractmethod
    def start(self, context): pass
  
    @abstractmethod
    def next_question(self, context): pass

# 2. Concrete States (구체적인 상태들)
class ReadyState(InterviewState):
    def start(self, context):
        print("🚀 면접을 시작합니다. '진행 중' 상태로 전환합니다.")
        context.set_state(InProgressState())
  
    def next_question(self, context):
        print("❌ 오류: 면접이 시작되지 않았습니다.")

class InProgressState(InterviewState):
    def start(self, context):
        print("❌ 오류: 이미 면접이 진행 중입니다.")
    
    def next_question(self, context):
        print("🎤 질문 추출 및 STT 분석 중... 다음 질문을 던집니다.")
        # 특정 조건(예: 질문 5개 완료) 시 평가 상태로 전환 가능
        # context.set_state(EvaluationState())

class EvaluationState(InterviewState):
    def start(self, context):
        print("❌ 오류: 이미 종료된 면접입니다.")
    
    def next_question(self, context):
        print("📊 면접이 끝났습니다. 현재는 결과 리포트를 생성하는 단계입니다.")

# 3. Context (컨텍스트: 면접 세션)
class InterviewSession:
    def __init__(self):
        self._state = ReadyState()  # 초기 상태: 대기

    def set_state(self, state: InterviewState):
        self._state = state

    def start_interview(self):
        self._state.start(self)

    def request_question(self):
        self._state.next_question(self)

# --- 클라이언트 사용 코드 ---
session = InterviewSession()

# 1. 대기 상태에서 질문 요청 시도
session.request_question() 

# 2. 면접 시작 (상태가 Ready -> InProgress로 변경됨)
session.start_interview()

# 3. 진행 상태에서 질문 요청
session.request_question()

# 4. 상태 강제 변경 (평가 단계로 진입)
session.set_state(EvaluationState())
session.request_question()
```

---

### 4. 상태(State) 패턴 vs 전략(Strategy) 패턴

두 패턴은 구조가 거의 같아 헷갈리기 쉽지만, **의도**가 다릅니다.

| **패턴**            | **핵심 의도**             | **특징**                                           |
| ------------------------- | ------------------------------- | -------------------------------------------------------- |
| **상태 (State)**    | **상태에 따른 행동 변화** | 상태들이 서로의 존재를 알며**자동으로 전환**됨     |
| **전략 (Strategy)** | **알고리즘 교체**         | 클라이언트가 필요에 따라**수동으로 전략을 선택**함 |

---

### 💡 프로젝트 적용 팁

사용자님이 진행 중인 **AI 모의 면접 프로젝트**에서, 면접자가 답변을 하는 동안의 시스템 상태를 세밀하게 나누어 보세요.

* **ListeningState** : 음성 입력 대기 및 실시간 STT 수행
* **ProcessingState** : LLM 분석 및 다음 질문 생성 중 (사용자에게 "분석 중입니다" 알림 표시)
* **SpeakingState** : TTS를 통해 질문을 읽어주는 중

이렇게 설계하면 각 단계에서 발생할 수 있는 예외 상황(예: 분석 중에 갑자기 답변을 시작하는 경우 등)을 상태별로 아주 안전하게 처리할 수 있습니다.

---

---

## 14. 커맨드 (Command) 패턴

**커맨드(Command) 패턴**은 요청(Request) 자체를 하나의 **객체로 캡슐화**하는 행위 디자인 패턴입니다.

쉽게 말해, "무엇을 실행할지"에 대한 정보를 담은 '명령서'를 만드는 것입니다. 이렇게 하면 요청을 큐(Queue)에 쌓거나, 로그로 남기거나, 실행을 취소(Undo)하는 등의 작업을 매우 유연하게 처리할 수 있습니다.

---

### 1. 커맨드 패턴의 핵심 구조

* **Command (인터페이스):** 명령을 실행할 메서드(`execute`)를 정의합니다.
* **Concrete Command:** 특정 행동을 수행하기 위해 Receiver 객체와 액션을 연결합니다.
* **Receiver (수신자):** 실제 비즈니스 로직을 수행하는 객체입니다. (예: DB 저장 로직, AI 분석 로직)
* **Invoker (호출자):** 명령 객체를 보유하며, 언제 명령을 실행할지 결정합니다. (예: 버튼, 스케줄러)
* **Client:** 구체적인 커맨드 객체를 생성하고 Receiver를 할당합니다.

---

### 2. 왜 사용하는가? (장점)

1. **결합도 감소:** 작업을 요청하는 객체(Invoker)와 실제 작업을 수행하는 객체(Receiver)가 서로를 몰라도 됩니다.
2. **작업 취소/재실행 (Undo/Redo):** 명령 객체 내부에 이전 상태를 저장하면 실행 취소 기능을 쉽게 구현할 수 있습니다.
3. **작업 예약 및 큐잉:** 명령을 객체화했으므로 리스트에 담아 순차적으로 실행하거나 지연 실행이 가능합니다.
4. **단일 책임 원칙:** 작업을 유발하는 로직과 실제 작업 로직을 완벽히 분리합니다.

---

### 3. 실전 활용 사례: AI 면접 시스템의 '작업 관리자'

현재 개발 중인 시스템에서 면접자가 답변을 마치면  **[STT 변환]** ,  **[텍스트 분석]** , **[DB 결과 적재]** 같은 여러 작업이 순차적으로 일어나야 합니다. 이를 커맨드 객체로 만들어 관리해 보겠습니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# 1. Receiver: 실제 로직을 수행하는 객체
class InterviewSystem:
    def convert_stt(self):
        print("🎙️ STT: 음성을 텍스트로 변환 중...")

    def analyze_answer(self):
        print("🧠 AI: 답변 내용을 분석하고 점수를 계산합니다.")

    def save_to_db(self):
        print("💾 DB: 분석 결과를 PostgreSQL에 저장합니다.")

# 2. Command: 추상 클래스
class Command(ABC):
    @abstractmethod
    def execute(self): pass

# 3. Concrete Commands: 구체적인 명령들
class STTCommand(Command):
    def __init__(self, system: InterviewSystem):
        self.system = system
    def execute(self):
        self.system.convert_stt()

class AnalysisCommand(Command):
    def __init__(self, system: InterviewSystem):
        self.system = system
    def execute(self):
        self.system.analyze_answer()

class SaveCommand(Command):
    def __init__(self, system: InterviewSystem):
        self.system = system
    def execute(self):
        self.system.save_to_db()

# 4. Invoker: 명령을 실행하는 호출자 (매니저)
class TaskManager:
    def __init__(self):
        self._history = []

    def run_command(self, command: Command):
        command.execute()
        self._history.append(command)

# --- 클라이언트 사용 코드 ---
# 시스템(Receiver) 준비
interview_receiver = InterviewSystem()

# 작업 관리자(Invoker) 준비
manager = TaskManager()

# 수행할 작업들을 객체로 생성
tasks = [
    STTCommand(interview_receiver),
    AnalysisCommand(interview_receiver),
    SaveCommand(interview_receiver)
]

# 순차적으로 실행 (큐잉 처리와 비슷함)
print("=== 면접 답변 처리 프로세스 시작 ===")
for task in tasks:
    manager.run_command(task)
```

---

### 4. 커맨드 패턴 vs 전략(Strategy) 패턴

두 패턴은 모두 '객체를 주입받아 실행'한다는 점에서 비슷해 보이지만 의도가 다릅니다.

| **패턴**             | **핵심 의도**     | **특징**                                             |
| -------------------------- | ----------------------- | ---------------------------------------------------------- |
| **커맨드 (Command)** | **요청의 캡슐화** | "무엇을 할지"를 객체로 만들어 전달 (Undo, 로그, 큐잉 목적) |
| **전략 (Strategy)**  | **알고리즘 교체** | "어떻게 할지"에 대한 방법론을 런타임에 선택                |

---

### 💡 프로젝트 적용 팁

사용자님이 진행 중인 **모의 면접 AI 프로젝트**에서, 면접자가 답변을 완료한 후 시스템 부하가 심할 경우를 대비해 보세요.

* 답변 처리 요청들을 `Command` 객체로 만들어 **메시지 큐(예: RabbitMQ, Celery)**에 던져넣습니다.
* 서버 B(AI 서버)는 이 큐에서 커맨드 객체를 하나씩 꺼내어 `execute()`를 실행합니다.
  이렇게 하면 서버의 일시적인 과부하를 막고 시스템 안정성을 크게 높일 수 있습니다.

---

---

## 15. 템플릿 메서드 (Template Method) 패턴

**템플릿 메서드(Template Method) 패턴**은 알고리즘의 **구조(뼈대)**를 부모 클래스에서 정의하고, 알고리즘의 **구체적인 단계**들을 자식 클래스에서 재정의(Overriding)할 수 있게 하는 행위 디자인 패턴입니다.

쉽게 말해, "전체적인 작업 순서는 정해져 있지만, 세부적인 작업 내용은 상황마다 다를 때" 사용하는 패턴입니다.

---

### 1. 템플릿 메서드 패턴의 핵심 구조

이 패턴은 **상속**을 기반으로 하며, 부모 클래스가 제어권을 가집니다.

* **Abstract Class (추상 클래스):** 알고리즘의 골격을 정의하는 `template_method`를 가집니다. 이 안에는 공통 단계와 자식이 구현해야 할 추상 메서드들이 포함됩니다.
* **Concrete Class (구체 클래스):** 부모 클래스에서 선언된 추상 메서드들을 구현하여 특정 단계의 알고리즘을 완성합니다.
* **Hook (훅):** 부모 클래스에서 구현되어 있지만 빈 내용이거나 기본값이 있는 메서드로, 자식 클래스에서 필요에 따라 선택적으로 오버라이딩할 수 있는 지점입니다.

---

### 2. 왜 사용하는가? (장점)

1. **코드 중복 제거:** 공통된 로직은 부모 클래스에 한 번만 작성하므로 유지보수가 쉽습니다.
2. **프레임워크 설계:** 전체적인 흐름은 고정하되 세부 구현만 사용자에게 맡기는 라이브러리나 프레임워크 설계에 필수적입니다.
3. **확장성:** 기존 알고리즘의 구조를 건드리지 않고 새로운 세부 동작을 추가하기 용이합니다.

---

### 3. 실전 활용 사례: AI 면접 데이터 처리 파이프라인

사용자님이 구축 중인 **AI 모의 면접 시스템**에서, 영상 답변을 처리하는 과정은 항상 [데이터 로드] → [분석] → [결과 저장] 순서를 따릅니다. 하지만 분석 단계에서 **STT 기반 텍스트 분석**을 할지, **비전 기반 표정 분석**을 할지는 달라질 수 있습니다.

#### Python 예제 코드

**Python**

```python
from abc import ABC, abstractmethod

# 1. Abstract Class: 알고리즘의 뼈대를 정의
class InterviewDataProcessor(ABC):
    def process(self):
        """템플릿 메서드: 전체 공정을 정의합니다."""
        self.load_data()
        self.analyze()      # 자식이 구현할 핵심 단계
        self.save_result()
        self.hook_after_process() # 선택적 확장 지점

    def load_data(self):
        print("📁 [Common] 영상/음성 데이터를 메모리에 로드합니다.")

    @abstractmethod
    def analyze(self):
        """추상 메서드: 자식 클래스에서 반드시 구현해야 함"""
        pass

    def save_result(self):
        print("💾 [Common] 분석 결과를 PostgreSQL 데이터베이스에 저장합니다.")

    def hook_after_process(self):
        """훅(Hook): 필요한 경우에만 자식에서 구현"""
        pass

# 2. Concrete Class A: 텍스트(STT) 분석기
class TextAnalysisProcessor(InterviewDataProcessor):
    def analyze(self):
        print("🧠 [Text] Whisper 모델을 사용하여 음성을 텍스트로 변환하고 키워드를 분석합니다.")

    def hook_after_process(self):
        print("📧 [Hook] 지원자에게 텍스트 분석 완료 알림을 보냅니다.")

# 2. Concrete Class B: 비전(얼굴) 분석기
class VisionAnalysisProcessor(InterviewDataProcessor):
    def analyze(self):
        print("👁️ [Vision] DeepFace 모델을 사용하여 표정과 시선 처리 점수를 계산합니다.")

# --- 클라이언트 사용 코드 ---
print("=== 텍스트 분석 파이프라인 가동 ===")
text_proc = TextAnalysisProcessor()
text_proc.process()

print("\n=== 비전 분석 파이프라인 가동 ===")
vision_proc = VisionAnalysisProcessor()
vision_proc.process()
```

---

### 4. 템플릿 메서드 vs 전략(Strategy) 패턴

두 패턴은 모두 알고리즘을 변경한다는 점에서 비슷하지만 구현 방식이 완전히 다릅니다.

| **구분**      | **템플릿 메서드 패턴**      | **전략 패턴**            |
| ------------------- | --------------------------------- | ------------------------------ |
| **핵심 기법** | **상속**(Inheritance)       | **합성**(Composition)    |
| **변경 범위** | 알고리즘의**일부분**만 변경 | 알고리즘**전체**를 교체  |
| **결합도**    | 부모-자식 간의 강한 결합          | 컨텍스트-전략 간의 느슨한 결합 |
| **유연성**    | 컴파일 타임에 결정됨              | 런타임에 동적으로 교체 가능    |

---

### 💡 프로젝트 적용 팁

현재 **서버 C**에서 **PostgreSQL과 Oracle**을 함께 다루고 계신데, 데이터 적재 로직을 템플릿 메서드로 설계해 보세요.

* `load_config()` (공통) → `connect_db()` (추상) → `insert_data()` (추상) → `close_connection()` (공통)
  이렇게 하면 새로운 종류의 DB가 추가되더라도 공통 로직(설정 로드, 예외 처리 등)은 재사용하고 연결/삽입 로직만 새로 작성하면 됩니다.

---

---

## 16. 리듀서(Reducer) 패턴


**리듀서(Reducer) 패턴**은 애플리케이션의 상태(State)를 변경하는 로직을 한곳으로 모으고, **"상태와 액션(Action)**  을 받아 새로운 상태를 반환"**하는 순수 함수를 이용하는 디자인 패턴입니다.

주로 React의 `useReducer`나 Redux에서 많이 사용되지만, 복잡한 상태 전환이 필요한 백엔드 로직이나 AI 파이프라인의 상태 관리에도 매우 효과적입니다.

---

### 1. 리듀서 패턴의 핵심 구성 요소

리듀서 패턴은 크게 세 가지 요소로 이루어집니다.

* **State (상태):** 애플리케이션의 현재 데이터(상태)입니다. 이 데이터는 직접 수정되지 않고 '읽기 전용'처럼 취급됩니다.
* **Action (액션):** 어떤 변화를 일으킬지 정의하는 객체입니다. 보통 `type`과 변화에 필요한 데이터인 `payload`를 포함합니다.
* **Reducer (리듀서):** 이전 상태와 액션을 입력받아 **완전히 새로운 상태 객체**를 계산해서 반환하는 함수입니다.

---

### 2. 왜 사용하는가? (장점)

1. **예측 가능성:** 동일한 상태와 액션이 주어지면 항상 동일한 결과 상태를 반환하므로 테스트와 디버깅이 쉽습니다.
2. **중앙 집중화:** 상태 변경 로직이 여러 곳에 흩어지지 않고 하나의 리듀서 함수 내에 모여 관리됩니다.
3. **상태 추적:** 상태가 변할 때마다 새로운 객체가 생성되므로, Arize Phoenix 같은 도구와 결합하여 상태 변화 이력을 로깅하기에 최적입니다.

---

### 3. 실전 활용 사례: AI 면접 세션 상태 관리

사용자님이 개발 중인 시스템에서 면접의 단계(대기, 녹음 중, 분석 중, 종료)를 관리하는 리듀서를 구현해 보겠습니다.

#### Python 예제 코드

**Python**

```python
from dataclasses import dataclass, replace
from typing import Optional

# 1. State 정의 (Immutable하게 설계)
@dataclass(frozen=True)
class InterviewState:
    status: str  # IDLE, RECORDING, ANALYZING, FINISHED
    current_question_idx: int
    error_message: Optional[str] = None

# 2. Reducer 함수 정의
def interview_reducer(state: InterviewState, action: dict) -> InterviewState:
    action_type = action.get("type")
    payload = action.get("payload", {})

    if action_type == "START_RECORDING":
        return replace(state, status="RECORDING", error_message=None)

    elif action_type == "FINISH_RECORDING":
        return replace(state, status="ANALYZING")

    elif action_type == "NEXT_QUESTION":
        new_idx = state.current_question_idx + 1
        return replace(state, status="IDLE", current_question_idx=new_idx)

    elif action_type == "SET_ERROR":
        return replace(state, status="ERROR", error_message=payload.get("message"))

    # 일치하는 액션이 없으면 현재 상태 그대로 반환
    return state

# --- 클라이언트 사용 코드 ---

# 초기 상태 설정
current_state = InterviewState(status="IDLE", current_question_idx=1)
print(f"초기 상태: {current_state}")

# 액션 발생 1: 녹음 시작
action_1 = {"type": "START_RECORDING"}
current_state = interview_reducer(current_state, action_1)
print(f"변경 후 1: {current_state}")

# 액션 발생 2: 녹음 종료 (분석 단계 진입)
action_2 = {"type": "FINISH_RECORDING"}
current_state = interview_reducer(current_state, action_2)
print(f"변경 후 2: {current_state}")

# 액션 발생 3: 에러 발생 시
action_3 = {"type": "SET_ERROR", "payload": {"message": "STT 서버 연결 실패"}}
current_state = interview_reducer(current_state, action_3)
print(f"변경 후 3: {current_state}")
```

---

### 4. 💡 프로젝트 적용 팁 (서버 A & Arize Phoenix)

현재 서버 A에서 전체 면접 흐름을 관리하고 계신데, 이 리듀서 패턴을 **Arize Phoenix**와 결합해 보세요.

* **상태 스냅샷 로깅:** 리듀서가 새로운 상태를 반환할 때마다 해당 상태 객체를 Phoenix에 로그로 남깁니다.
* **버그 추적:** "사용자가 어느 단계에서 에러를 겪었는가?"를 파악할 때, Phoenix 대시보드에서 액션(Action) 흐름만 따라가면 정확한 버그 발생 지점을 찾아낼 수 있습니다.
* **공유 폴더(Z:) 활용:** 각 상태 변화 시점의 요약본을 JSON 형태로 공유 폴더에 저장해두면, 서버 C의 배치 프로그램이 이를 읽어 통계를 내기 매우 좋습니다.
