version: '3.8'

services:
  backend:
    build: .
    container_name: mock_interview_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
